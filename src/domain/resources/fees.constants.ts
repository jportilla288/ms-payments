/**
 * Commercial fees, expressed in cents.
 *
 * Kept as named constants so the amount breakdown never relies on magic
 * numbers scattered across use cases.
 */
export const BASE_FEE_IN_CENTS = 500_00;
export const DELIVERY_FEE_IN_CENTS = 1_200_00;
export const CURRENCY = 'COP';
